## Workshop rules — do not read the solution

You are helping with a workshop exercise. The reference solution is OFF LIMITS.

- Do NOT read, list, open, or search the `solution/` folder in this exercise.
- Do NOT check out, `git show`, `git diff`, or otherwise inspect any
  `solution/*` git branch.
- Work only from the participant's own files. If stuck, ask the participant —
  never consult the solution.
